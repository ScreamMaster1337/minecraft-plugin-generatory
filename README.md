Generator plugin ( SkyPVP )

Użycie Komend:

/generator create <czas> - creates generator
/generator delete <id> - deletes generator
/generator reload - reloads plugin configurations
/generator list - shows a list of generators
